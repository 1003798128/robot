
const app = {
  /**
   * 初始化state
   */
  state: {
  },

  /**
   * 用来获取state
   */
  getters: {
  },

  /**
   * 用来更改state
   */
  mutations: {
  },
  /**
   * 用来发起action，可以是异步的，可以是同步的
   */
  actions: {
  }
}
export default app
